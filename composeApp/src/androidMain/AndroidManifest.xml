<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- In-app updates: the app fetches its own APK and hands it to the OS.
         A device-owner handset is exempt from this permission, but the same APK
         also runs on handsets that were never provisioned, where it is what
         allows the confirm dialog to appear at all. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Location permissions and the tracking service are declared by the `gms`
         flavour manifest; the `nogms` build has neither. -->

    <!-- Thermal printer (XPrinter SDK): Bluetooth / USB connectivity -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        tools:targetApi="s" />
    <!-- The SDK manifest marks usb.host as required; relax it so the app installs everywhere -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false"
        tools:replace="android:required" />

    <application
        android:name=".FlowVanApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ─── Self-update ────────────────────────────────────────────────
             See docs/auto-update.md. The admin receiver exists only so that
             `adb shell dpm set-device-owner` has a component to name; being
             device owner is what makes the install silent. -->
        <receiver
            android:name=".admin.FlowVanDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Where the OS reports what became of an install session. Not exported:
             the only sender is our own PendingIntent. -->
        <receiver
            android:name="com.jehadalomour.flowvan.core.data.update.UpdateInstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.jehadalomour.flowvan.INSTALL_STATUS" />
            </intent-filter>
        </receiver>

        <!-- Puts the app back on screen after it has replaced itself. -->
        <receiver
            android:name=".admin.UpdateRelaunchReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
