package com.jehadalomour.flowvan

import android.app.Application

/** Nothing from Google is linked into this build, so there is nothing to start. */
internal fun Application.initPlatformServices() = Unit
